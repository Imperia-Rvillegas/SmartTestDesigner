/**
 * Contiene las enumeraciones utilizadas en el proyecto.
 * <p>
 * Este paquete agrupa todos los {@code enum} que representan
 * constantes bien definidas, como estados, tipos, roles u opciones
 * específicas dentro del sistema. Centralizar las enumeraciones
 * permite mantener el código más legible y fácil de mantener.
 * </p>
 *
 * <h2>Ejemplos de uso</h2>
 * <pre>
 *   // Ejemplo de referencia a una enumeración
 *   Status estado = Status.ACTIVO;
 *
 *   if (estado == Status.ACTIVO) {
 *       System.out.println("El usuario está activo");
 *   }
 * </pre>
 *
 */
package ui.enums;