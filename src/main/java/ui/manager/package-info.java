/**
 * Este paquete centraliza la gestión de instancias de páginas (Page Objects) y utilidades
 * como navegación, manejo de ventanas emergentes (popups) y esperas explícitas.
 *
 * La clase principal es PageManager, que implementa el patrón Singleton para facilitar
 * el acceso a objetos compartidos dentro del flujo de pruebas.
 */
package ui.manager;