/**
 * Este paquete define clases que se ejecutan antes o después de los escenarios
 * de prueba (hooks), como la inicialización del navegador o el cierre del driver.
 *
 * Se integra con Cucumber a través de anotaciones como @Before y @After.
 */
package hooks;