/**
 * Este paquete contiene las clases encargadas de realizar llamadas a APIs REST.
 *
 * Cada clase representa una API específica e incluye métodos para enviar solicitudes
 * (GET, POST, PUT, DELETE) y manejar las respuestas correspondientes.
 */
package api;